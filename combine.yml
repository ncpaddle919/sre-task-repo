apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-alertmanager
data:
  alertmanager.yml: |
    global:
      resolve_timeout: 1m

    receivers:
    - name: 'notifications'
      email_configs:
      - to: dbwagoner@gmail.com
        from: dbwagoner@gmail.com
        smarthost: smtp.gmail.com:587
        auth_username: dbwagoner@gmail.com
        auth_identity: dbwagoner@gmail.com
        auth_password: wkbr etun ocqb cqpq
        send_resolved: true
        headers:
          subject: "Prometheus - Alert"
          text: "{{ range .Alerts }} Hi, \n{{ .Annotations.summary }}\n{{ .Annotations.description }} {{end}}"

      slack_configs:
      - channel: '#upcommerce-devs'
        send_resolved: true
        api_url: 'https://hooks.slack.com/services/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'

    route:
      group_wait: 10s
      group_interval: 2m
      receiver: 'notifications'
      repeat_interval: 2m